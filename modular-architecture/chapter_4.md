## 🌳 现代 React 项目的目录结构（DDD 版本）
```pyhton
src/
├── app/                     # 应用入口（路由、布局、lazy config）
│   ├── App.tsx
│   ├── routes/
│   │   ├── index.tsx
│   │   ├── dashboard/
│   │   │   ├── index.tsx
│   │   │   └── lazy.ts     # 懒加载入口
│   │   └── user/
│   │       ├── index.tsx
│   │       └── lazy.ts
│   └── providers/           # 全局 Provider（QueryClient、Stores、Theme）
│       ├── QueryProvider.tsx
│       ├── ThemeProvider.tsx
│       └── StoreProvider.tsx
│
├── domain/                  # 业务领域层（最重要）
│   ├── user/
│   │   ├── model.ts         # 领域逻辑（Types、state machines、计算逻辑）
│   │   ├── service.ts       # 调用 infra 层
│   │   └── index.ts
│   ├── order/
│   │   ├── model.ts
│   │   ├── service.ts
│   │   └── index.ts
│   └── product/
│       ├── model.ts
│       ├── service.ts
│       └── index.ts
│
├── ui/                      # UI 组件库（无业务）
│   ├── Button/
│   │   ├── Button.tsx
│   │   ├── Button.style.ts
│   │   └── index.ts
│   ├── Table/
│   ├── Form/
│   └── ...
│
├── infra/                   # 基础设施层（可被领域使用）
│   ├── http/
│   │   ├── client.ts        # axios/fetch wrapper
│   │   ├── interceptors.ts
│   │   └── index.ts
│   ├── storage/
│   │   ├── local.ts
│   │   └── session.ts
│   ├── config/
│   │   ├── env.ts
│   │   └── constants.ts
│   └── logger/
│       └── index.ts
│
├── features/                # 组合 UI + 业务的“小场景模块”
│   ├── LoginForm/
│   │   ├── LoginForm.tsx
│   │   └── index.ts
│   ├── ProductCard/
│   └── DashboardCharts/
│
├── utils/                   # 小工具函数（无副作用）
│   ├── format.ts
│   ├── math.ts
│   └── index.ts
│
├── hooks/                   # 通用 hooks
│   ├── useDebounce.ts
│   ├── usePagination.ts
│   └── index.ts
│
├── styles/                  # 全局样式
│   ├── variables.css
│   ├── resets.css
│   └── index.css
│
└── types/                   # 全局 TypeScript 类型
    ├── api.ts
    ├── entities.ts
    └── index.ts
```

---

## 🌈 结构解读（符合你现在的认知模型）

---

## 1. domain 层 = 业务核心（不可依赖 UI）

这里是：

- 数据结构
- 业务规则
- 计算逻辑
- state machine
- DTO / model / types
- service（infra 的调用封装）

**domain 不能 import ui**  
**domain 不能 import app**  
**domain 只依赖 infra & utils**

这和你非常喜欢的「结构化思维」高度一致。

---

## 2. ui 层 = 无业务的纯组件库

UI 必须是：

- 无业务逻辑
- 可复用
- 纯渲染
- props → UI（pure function）

你可以把 ui 层视为「小 React Fiber 子树模板」。

---

## 3. infra 层 = 业务无关的副作用封装

包含：

- http 客户端
- 存储
- env config
- 日志

domain 和 app 都能调用 infra。

infra 只做一件事：

> 把混乱的外部世界（HTTP、浏览器存储）包装成稳定的 API。

它非常像我们之前说的：

> 对副作用进行函数式封装。

---

## 4. features 层 = UI + domain 的组合模块

比如一个 ProductCard：

- UI（卡片布局）
- 业务渲染（用 domain.product 的数据）

它是「组件化业务场景」。

业务不够大，不需要独立 domain → 放 features。

---

## 5. app 层 = glue（胶水）层

包括：

- Router
- Layout
- Providers
- App.tsx

它的职责就是：

> 把 domain / ui / infra 粘（粘）在一起，形成一个完整应用。

---

## ✨ 为什么这个结构适合你？

因为它天然是一个「依赖图（Graph）」：

```text
infra
  ↑
domain
  ↑
features
  ↑
ui
  ↑
app
```

并且：

> **lazy subgraph（懒加载）可以天然落在 routes / features / big UI 组件上。**
